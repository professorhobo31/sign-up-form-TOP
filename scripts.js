// Keep the js SIMPLE to add a "Passwords don-t match" warning and functionality to the submit button

let password1 = '';
let password2 = '';

const submitButton = document.getElementById("submitButton");

function submitForm() {
    const form = document.getElementById("signUpForm");
    alert('click')
}